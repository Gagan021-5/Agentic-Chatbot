# Background Removal Tool Blueprint

This tool handles removing the background from a source product image or portrait to make it transparent or replace it.

Key Phrases: background removal, remove background, transparent, cutout, rmbg, background colors.

```json
{
  "tool_id": "bg_remover",
  "show_upload": true,
  "show_url_input": true,
  "layout_mode": "interactive",
  "config": {
    "api_provider": "remove.bg",
    "output_format": "png",
    "active_tool": "bg_remover"
  }
}
```
